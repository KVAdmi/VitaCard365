pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

plugins {
    // Si ya tienes versiones declaradas, déjalas; si no, no agregues nada aquí.
}

apply from: "capacitor.settings.gradle"

// --- GUARD: solo cargar Cordova si el archivo settings.gradle existe ---
def cordovaSettings = new File(rootDir, "capacitor-cordova-android-plugins/settings.gradle")
if (cordovaSettings.exists()) {
    apply from: cordovaSettings
}
// --- FIN GUARD ---

// Core app module
include ':app'

// Capacitor core (redundante si apply-from ya lo incluye, pero no falla si repite)
include ':capacitor-android'
project(':capacitor-android').projectDir = new File('..\\node_modules\\@capacitor\\android\\capacitor')