# Forzar R8 y habilitar mapping
android.enableR8=true
android.enableR8.fullMode=true
android.suppressUnsupportedCompileSdk=34
# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
android.enableJetifier=true
VITA_STORE_FILE=vitacard-release.jks
# IMPORTANT: secrets below removed from repository. Please set these locally
# in a non-committed file or environment variables before building/releasing.
# Example (PowerShell):
#   $env:VITA_STORE_PASSWORD = 'your-password'
#   $env:VITA_KEY_PASSWORD = 'your-key-pass'
# The build system should read them from env or a local gradle.properties excluded from git.
VITA_STORE_FILE=C:\Users\Victor Guerra\OneDrive - Azisted\Documentos\Kv Apss\VitaCard365\Keys\my-new-upload-key.jks
VITA_STORE_PASSWORD=Vp120125@
VITA_KEY_ALIAS=upload
VITA_KEY_PASSWORD=Vp120125@
APP_VERSION_CODE=48
APP_VERSION_NAME=1.0.10
android.suppressUnsupportedCompileSdk=

kotlin.compiler.jvmTarget=17
# Force Gradle toolchain to use the provided JDK21 on this machine
# org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.8+9
# Commented out so Gradle will use the system JAVA_HOME or its bundled toolchain.
# If you need to force a specific JDK, set org.gradle.java.home to a valid path on your PC.
# Prefer a Java 17 runtime for Gradle to avoid unsupported class file versions.
# Adjust this path if your JDK 17 is installed elsewhere.

