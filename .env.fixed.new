# Variables de entorno para VitaCard365 - Configuración corregida

# Frontend (Vite)
VITE_MP_PUBLIC_KEY=your_mercadopago_public_key_here
VITE_API_BASE=http://localhost:3000
FRONTEND_BASE_URL=http://localhost:5174

# Backend (Node.js/Express)
MP_ACCESS_TOKEN=your_mercadopago_access_token_here
PORT=3000
NODE_ENV=development

# URLs de redirección
PUBLIC_URL=http://localhost:5174
VITE_API_BASE_URL=http://localhost:3000

# Configuración de CORS
ALLOWED_ORIGINS=http://localhost:5174,http://localhost:3000,https://vitacard365.com,https://www.vitacard365.com

# Notas importantes:
# 1. Reemplaza 'your_mercadopago_public_key_here' con tu clave pública de MP
# 2. Reemplaza 'your_mercadopago_access_token_here' con tu token de acceso de MP
# 3. Para producción, cambia las URLs localhost por las URLs reales
# 4. Asegúrate de que el puerto del backend (3000) esté disponible
# 5. El frontend debe ejecutarse en el puerto 5174 (Vite default)
